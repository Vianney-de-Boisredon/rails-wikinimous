<%= form_for(@article) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.text_field :content %>
  <% unless @article.new_record? %>
  <%= f.label :read? %>
  <%= f.check_box :read %>
  <% end %>
  <br>
  <%= f.submit %>
<% end %>
